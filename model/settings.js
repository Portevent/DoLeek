// Shared application settings
export const settings = {
    rangeMode: false,
    perTpMode: false, // show effect values divided by the item's TP cost
    critMode: 'never', // 'never' | 'average' | 'always'
};
